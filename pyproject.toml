[project]
name = "typequest"
version = "0.1.0"
requires-python = "~=3.12.0"

[tool.vercel.scripts]
build = "cd frontend && npm install && npm run build && rm -rf ../public && mkdir -p ../public && cp -r dist/* ../public/"
